# Copy this file to `.env` and fill in your real values.
# DO NOT commit `.env` — it is git-ignored.

# --- API ---
# Demo points to JSONPlaceholder (public, stable, no auth) — replace with your backend.
API_MAIN_URL=https://jsonplaceholder.typicode.com
API_PORTAL_URL=https://jsonplaceholder.typicode.com

# --- Tracking ---
SENTRY_DSN=
